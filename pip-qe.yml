---
- PIP:
    - EXTRA_INDEX_URL: https://migration.jenkins.cit.rackspace.net:3143/faws-migration-svc/qe/+simple/
    # To use a single PyPI, other than the default pypi.python.org, eg limit
    # packages to a QE-release-qualified DevPI index:
    # * Comment out EXTRA_INDEX_URL.
    # * Set INDEX_URL, eg...
    #- INDEX_URL: https://devpi.rackspace.net/faws-migration/qe/+simple/
    #
    # To have access to internally distributed dependencies while disconnected
    # from the Rackspace network:
    # * Cache internally distributed dependencies before disconnecting.
    # pip download \
    #   --extra-index-url https://migration.jenkins.cit.rackspace.net:3143/faws-migration-svc/qe/+simple/ \
    #   --no-deps \
    #   --dest ./cache/ \
    #   migrator-common ...
    # * Comment out EXTRA_INDEX_URL.
    # * Set FIND_LINKS, eg...
    #- FIND_LINKS: ./cache/
